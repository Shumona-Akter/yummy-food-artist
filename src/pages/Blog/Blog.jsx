import React from 'react';

const Blog = () => {
    return (
        <div>
           <h5>1. Tell us the differences between uncontrolled and controlled components.</h5>
           <p>ans : In React, controlled components refer to components that have their state and behavior controlled by the parent component. These components rely on props passed down from the parent component to update their state and behavior. </p>
           <h5>1. Tell us the differences between uncontrolled and controlled components.</h5>
           <p>ans : In React, controlled components refer to components that have their state and behavior controlled by the parent component. These components rely on props passed down from the parent component to update their state and behavior. </p>
           <h5>1. Tell us the differences between uncontrolled and controlled components.</h5>
           <p>ans : In React, controlled components refer to components that have their state and behavior controlled by the parent component. These components rely on props passed down from the parent component to update their state and behavior. </p>
        </div>
    );
};

export default Blog;